# Contributing

1. Create a feature branch.
2. Run `npm install`.
3. Add or update tests for business logic.
4. Run `npm run test`, `npm run typecheck`, and `npm run build`.
5. Keep resume facts truthful. Do not add features that fabricate achievements, hidden keywords, or unsupported skills.
6. Do not commit secrets or real user resumes.
